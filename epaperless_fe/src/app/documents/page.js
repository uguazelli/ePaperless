"use client";

import Layout from "@/app/components/layout";
import ImageGrid from "@/app/documents/imageGrid";

const Documents = () => {
  return <Layout workspace={<ImageGrid />} />;
};

export default Documents;
